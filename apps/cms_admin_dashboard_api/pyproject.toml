[project]
name = "cms-admin-dashboard-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["cms-common", "fastapi[standard]", "logto", "pyjwt[crypto]"]

[tool.uv.sources]
cms-common = { workspace = true }
